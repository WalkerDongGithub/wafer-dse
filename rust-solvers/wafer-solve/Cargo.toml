[package]
name = "wafer-solve"
version = "0.1.0"
edition = "2021"
description = "Unified CLI for wafer solver algorithms — the binary Python calls"

[dependencies]
wafer-core = { path = "../wafer-core" }
wafer-hungarian = { path = "../wafer-hungarian" }
wafer-derangement = { path = "../wafer-derangement" }
