[package]
name = "wafer-core"
version = "0.1.0"
edition = "2021"
description = "Shared types and JSON I/O protocol for wafer-solver crates"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
