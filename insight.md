# Critical Insight

注意！：agent等智能体工具严禁修改本文内容！


本文着重搜集笔者在制作该代码时所总结的一些关键洞察，用于日后撰写文章使用。

## 1. 筛选而非优化

全文设计空间探索其目的不在于找到某种Pareto平面，更旨在筛选某一种目标架构满足严苛但又基础的条件：无阻塞、物理可行等。

## 2. 突破DSE的二元范式，通过一个指标来量化一个解的质量

DSE不光要给一个解是否可行的判断，还要给多大程度可行的判断。这是现存DSE共有的一个问题，解的质量要进行量化，而交换机的性能量化则更困难。

本文则给出一个方法，通过设计点承诺额定带宽来作为量化指标。在同一组DSE下，相同端口数量，能够承诺更高额定带宽的交换机设计点，解的质量就优于只能承诺更低额定带宽的交换机设计点。

## 3. B* = f(要求, 约束)

本文认为，一个设计点是否可行，其实取决于两个部分：
1. 我们对解的要求有多严格。例如，对一台交换机，额定带宽往往标定了一个具体的设计工况，例如额定带宽A标准保证了在所有端口保持额定带宽以下时，交换机可以保证无阻塞交换能力，某端口高于额定带宽时则不再有如此保证；再例如额定带宽B不保证无阻塞，只标注出入口的最大带宽。这就是两种完全不同的性能要求，而且明显A要比B严格的多。在两种要求下，同样一台物理交换机，在A要求下会被判定为不可行，而B要求下则可行。
2. 我们的物理约束有多悲观。例如，我们可能需要考虑极端工况A，认为交换机存在一种所有节点保持满负荷峰值带宽的运行状态；而另一种考虑B则认为，或许没有那么糟糕，我们只需要考虑交换节点的额定功耗即可。这又是两种不同的约束，同样的，在这两种约束下，交换机能够达到的额定带宽又有不同，如果我们必须考虑满负荷峰值带宽的工况，那么对应的安全阈值下的额定带宽就会相应降低。反之如果我们拒绝考虑峰值功耗而只考虑额定功耗，那么我们能承诺的额定带宽就会相应的推高。因为我们认为贪图带宽而选择把机器跑坏是使用者的责任。。

这两个考量都影响着解的选取，因此为设计点标定的带宽值总是伴随着背后的权衡，更高的要求和更严的约束，意味着我们只能标注更低的带宽来满足二者，反之则可以选择适度推高。换而言之，B 是要求与约束的单调函数。通过放宽或收紧二者可以得到几种完全不同的DSE模型。从而帮助设计者灵活决策。从多维度考虑问题。

## 4. 多因素耦合

DSE不是算数，太多因素都要考虑在内，一台晶圆级交换机需要考虑的因素有很多，拓扑、路由、功耗、面积、信号完整性、封装方式、节点布局，更重要的是，各方的耦合关系复杂，很难综合决策。前人的研究多才去各因素分离的决策，本文提出一种多因素耦合量化方法，综合热、电、几何、性能等多位一体的综合决策手段，并在经典拓扑结构下保证多项式时间全局最优解。

## 5. DSE解出B不单是为了控制解的质量，更是后续精调的量化基石

DSE工具终究是数学抽象，不能100%反应真实的物理结构，更多的性能调试还是要靠仿真验证等手段来做最终推演，但是本文的DSE依旧展示了它的强大先验知识搜集能力，DSE工具解出的结构往往是在相对更严格的约束上进行的，当一个结构在严格的条件下距离我们的预期目标较近，那么说明该结构完全有可能在放宽的真实物理环境下变成可行解，这就是量化的威力，决策权从DSE交给了设计师，设计师可以对设计点进行“排序”，根据解的质量一个一个论证设计点的可行性。即便在更严格DSE中所有的点都在预期以下，设计师依然可以按照解质量的顺序从高到底依次论证。

## 6. 具体模型的insight： 扩展比联系一切

联系物理与拓扑的桥梁是每条链路的lane数量，lane越多，铺设的线路带宽越大，所要使用的几何资源、功耗资源就越多。但如果将真实带宽完全作为拓扑性能的物理量与物理拓扑综合考虑，计算起来非常困难，因此本文想到一个好办法（先这么说吧先。），将带宽分成：出入口预期额定带宽*相对额定带宽的扩展比（也称加速比），例如入口处的额定带宽2Gbps，某条链路为3Gbps，那么模型将其表示为 2Gbps * 1.5，所有链路的扩展比只和拓扑结构本身以及设计师对拓扑结构的具体要求有关。

我们称剥离了出入口额定带宽的由每条链路的扩展比向量成为扩展比包络，用于表示某一个拓扑为了满足某一个性能要求必须保证的最小扩展比。例如，如果在无阻塞的要求下，一条链路在某种调度下最多可能要承担2倍于额定带宽的流量，那么该链路的扩展比将至少为 2.每一个拓扑在某一个要求下有唯一的包络，并且只与拓扑本身的属性有关，而与物理工况无关，因此可以单独计算得到。

而物理约束则是通过具体的带宽来进行设计权衡的。



## 7. 全局最优解的讨论价值远好于普通的解

芯片布局、拓扑选择等问题都属于chiplet界经典的NP 难问题，但是根据调研，我们发现晶圆级系统的很大一部分问题其实并不是NPHard问题，至少在讨论一些尽可能严格、或尽可能松的极端约束下，并不是，甚至是一个好解的问题。最经典的例子是晶圆级系统的热分析，在不考虑所有的功耗条件，只考虑额定功耗或峰值功耗的条件下，基于一系列物理定律，系统稳态温度总是与功耗保持线性关系，区别只在于不同的封装方式和传热分析粒度下，其温度与功耗的线性关系矩阵该如何表示。因此本文由此获得灵感，大量的设计约束，实质上都可以被表示为扩展比的线性约束。从而将大量的约束点都通过纯LP（不是MILP 就是LP）配合一些经典算法来获得满足全部约束的全局最优解。在全局最优解下讨论解的质量与灵敏度分析。设计者可以通过求解LP来获得某一个设计点的全部设计指南。

